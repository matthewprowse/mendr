/**
 * @deprecated — all exports here were unused. File kept as a placeholder to avoid
 * import errors. Remove once confirmed no external references remain.
 *
 * OUTPUT_FORMAT_CRITICAL_BLOCK, OUTPUT_FORMAT_STREAMING_BLOCK, OUTPUT_FORMAT_COST_BLOCK,
 * and buildOutputFormatJsonExampleBlock were never imported by any active module.
 * The live output-format instructions live in output-format.ts (OUTPUT_FORMAT_PROMPT_BLOCK).
 */
