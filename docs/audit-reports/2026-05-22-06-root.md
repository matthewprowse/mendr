# A6 — Root-level and Non-src/ Audit

**Date:** 2026-05-23
**Phase:** Root files, scripts, migrations, configs, supabase, emails, public assets, .github, .claude, repo-root
**Scope:** Everything outside `src/` in `app/`, plus repo-root level

---

## Pre-audit Known Drift Confirmation

✅ Confirmed: **Broken git refs** (`main 2`, `main 3`, `main 4`) exist at `app/.git/refs/heads/`.
✅ Confirmed: **Planning docs** in `app/public/docs/planning/` are publicly served by Next.js (3 files: `ai-coding-improvements.md`, `coding-standards.md`, `launch-checklist.md`).

---

## Findings (32 total)

### Severity: HIGH

#### 1. Corrupt Git Refs (6 instances)
**Path:** `app/.git/refs/heads/`
**Details:** OS-created duplicate branch refs with spaces in filenames:
- `main 2` → commit `5bbd5b5d788c5775a980d7fa2935eccab761c1c4`
- `main 3` → commit `e74442f5a9fc8a7c8ad92a83650dbe516c65ce26`
- `main 4` → commit `36bfd08f3f93387aac8e36eac90c6fbb0567dd2f`
**Severity:** HIGH. These are broken refs that should be safely removed. Each has a valid commit SHA but the branch names with spaces violate git conventions and cause issues with tools. Recommend: `rm "app/.git/refs/heads/main 2" "app/.git/refs/heads/main 3" "app/.git/refs/heads/main 4"`.

#### 2. Lockfile Conflict: Both pnpm-lock.yaml and package-lock.json Exist
**Paths:** 
- `app/pnpm-lock.yaml`
- `app/package-lock.json`
**Details:** CLAUDE.md specifies pnpm as the package manager and explicitly forbids committing `package-lock.json` or `yarn.lock`. The presence of both indicates a merge conflict or incomplete lockfile migration. CI uses pnpm exclusively (see `.github/workflows/test.yml`).
**Severity:** HIGH. Package lockfile mismatch can cause dependency resolution issues and CI surprises. Recommend: Delete `package-lock.json` and ensure only `pnpm-lock.yaml` is committed.

#### 3. Planning Docs Publicly Served by Next.js
**Path:** `app/public/docs/planning/`
**Files:**
- `ai-coding-improvements.md` (17.6 KB)
- `coding-standards.md` (46.5 KB)
- `launch-checklist.md` (18.4 KB)
**Details:** These internal planning documents are served as static assets under Next.js and are publicly accessible. If they contain sensitive information (roadmaps, internal constraints, secret URLs, API strategies), they pose an information leakage risk.
**Severity:** HIGH (if sensitive content) / MEDIUM (if generic). Recommend: Move to `app/docs/` (not publicly served) and remove from `public/`. Verify content for sensitivity before deciding.

---

### Severity: MEDIUM

#### 4. OS-Duplicate Migrations (6 instances with " 2" suffix)
**Path:** `app/supabase/migrations/`
**Duplicates:**
- `20260407000000_rename_conversations_and_clean_providers 2.sql` (90 lines)
- `20260410180000_market_rates_cache 2.sql` (23 lines)
- `20260413120000_data_layer_indexes_reviews_unique 2.sql` (34 lines)
- `20260413120001_data_layer_providers_soft_delete_usage_timestamps 2.sql` (31 lines)
- `20260413120002_data_layer_jsonb_history_maintenance 2.sql` (109 lines)
- `20260413120003_data_layer_rls_policies 2.sql` (125 lines)
**Details:** All duplicates have identical timestamps to their primary counterparts. Supabase applies migrations in lexicographic order, so `_2.sql` files are applied after their clean versions. The presence of both is a smell of incomplete deduplication after an OS copy/paste incident.
**Severity:** MEDIUM. The actual migrations will apply correctly (in order), but the duplicates clutter the schema and create confusion. Recommend: Delete all `* 2.sql` files. Verify that `supabase migration list` in production shows only the clean versions.

#### 5. Unreferenced Email Template
**Path:** `app/emails/monthly-digest-unregistered.tsx` (15 lines)
**Details:** This email template is not imported or referenced anywhere in `src/`. No API route, cron job, or service sends it.
**Severity:** MEDIUM. Dead code that should either be removed or connected to a sender. Recommend: Check git history to determine intent; if orphaned, delete.

#### 6. Missing Script References in package.json
**Scripts defined but not directly referenced:**
- `scrape-providers` (no file `app/scripts/scrape-providers.ts` exists; may be obsolete)
- `sync-provider-reviews` (referenced only in cron comment in `app/src/app/api/cron/sync-provider-reviews/route.ts`, not as a direct npm script caller)
**Details:** These are legitimate helper scripts or one-off utilities but are not documented in CLAUDE.md or used by CI. The fact that no corresponding `.ts` file exists for `scrape-providers` suggests the script is broken or the package.json entry is stale.
**Severity:** MEDIUM. Recommend: Verify whether `scrape-providers` script should exist; if not, remove from package.json. Verify `sync-provider-reviews` intent (appears to be a cron-only operation, not a manual script).

#### 7. Environment Variable Mismatches
**Missing from .env.example (in CLAUDE.md but not in .env.example):**
- `UPSTASH_REDIS_REST_URL`
- `UPSTASH_REDIS_REST_TOKEN`
- `NEXT_PUBLIC_SENTRY_DSN`
- `CRON_SECRET`
- `AI_PROVIDER_ENRICHMENT_VERSION`
- `RESEND_API_KEY`, `RESEND_FROM`, `RESEND_REPLY_TO`, `RESEND_ADMIN_EMAIL`
- `SEND_EMAIL_HOOK_SECRET`
- `DATAFORSEO_LOGIN`, `DATAFORSEO_PASSWORD`
- `GOOGLE_PLACES_API_KEY`

**Extra in .env.example (not in CLAUDE.md table):**
- `GEMINI_MODEL`
- `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_TYPES_TABLE`, `SUPABASE_SERVICE_TYPES_COLUMN` (these are aliases; CLAUDE.md uses NEXT_PUBLIC_* variants for browser)
- `OPENAI_API_KEY`
- `REDDIT_*` (4 vars: CLIENT_ID, CLIENT_SECRET, USERNAME, PASSWORD)
- `GOOGLE_CUSTOM_SEARCH_API_KEY`, `GOOGLE_CUSTOM_SEARCH_ENGINE_ID`
- `DISABLE_RATE_LIMIT`, `DISABLE_DIAGNOSIS_DAILY_QUOTA`
- `NEXT_PUBLIC_APP_URL`, `NEXT_PUBLIC_OPERATOR_*` (4 vars), `NEXT_PUBLIC_LEGAL_EMAIL`, `NEXT_PUBLIC_PRIVACY_EMAIL`, `NEXT_PUBLIC_INFORMATION_OFFICER_*` (2 vars), `NEXT_PUBLIC_GOOGLE_MAPS_*` (2 vars)
- `GOOGLE_APPLICATION_CREDENTIALS`, `GOOGLE_SPEECH_CREDENTIALS_JSON`
- `SENDGRID_*` (3 vars; note: CLAUDE.md lists RESEND, not SENDGRID — possible API provider change mid-project)

**Severity:** MEDIUM. CLAUDE.md is incomplete and .env.example has cruft from older phases (SENDGRID vs RESEND, Reddit scraper, OpenAI). The two files should be synchronized to prevent new developers from missing required variables. Recommend: Update CLAUDE.md with the complete current env var list; prune .env.example of unused variables (Reddit, OpenAI, SendGrid, disable flags if not used in this codebase phase).

---

### Severity: LOW

#### 8. Empty README
**Path:** `app/README.md`
**Details:** File exists but is empty (1 line, no content).
**Severity:** LOW. Not critical but should either document the app or be deleted.

#### 9. Font Files with Spaces in Names (5 files)
**Path:** `app/public/fonts/`
**Files:**
- `Soehne Leicht.otf`
- `Soehne Extrafett.otf`
- `Soehne Halbfett.otf`
- `Soehne Kraftig.otf`
- `Soehne Dreiviertelfett.otf`
**Details:** All font files have spaces in their names. While browsers handle this correctly, it can cause issues in CI scripts and asset processing pipelines that naively split on whitespace.
**Severity:** LOW. These are safely served as-is by Next.js, but following CLAUDE.md's "never use spaces in file names" convention, consider renaming to kebab-case (e.g., `Soehne-Extrafett.otf`) and updating CSS imports.

#### 10. Unused TypeScript Scripts (likely candidates)
**Path:** `app/scripts/`
**Candidates for deletion or deprecation:**
- `diagnosis-sample-export.ts` (49 lines, Apr 14) — not referenced anywhere
- `re-enrich-providers.ts` (148 lines, May 20) — not referenced anywhere
- `test-enrichment-output.ts` (188 lines, May 20) — not referenced anywhere
- `test-safe-redirect.ts` (62 lines, May 12) — not referenced anywhere
**Details:** All four are one-off utility or test scripts. No imports, no CI references. They may be intentionally kept "just in case" but should be documented if they are.
**Severity:** LOW. These are not in any critical path, but they consume space and mental load. Recommend: If they served their purpose, delete. If they're intended as escape hatches for manual debugging, document in a `scripts/README.md` with one-liner descriptions.

#### 11. Python Scripts in Repo Root (not referenced in CI)
**Path:** `scripts/` (repo-root)
**Files:**
- `reddit_dataset_scraper.py` (18 KB, May 20)
- `validate_reddit_dataset.py` (1.5 KB, May 22)
- `watch_metrics.py` (4.2 KB, May 20)
**Details:** These appear to be data pipeline scripts for the Reddit dataset training set. Not referenced in `.github/workflows/test.yml`. Unclear if they're still actively maintained or if this data pipeline is still used.
**Severity:** LOW. External to the Next.js app. Recommend: Verify with owner whether this pipeline is still active; if not, consider moving to an archive or data/ subfolder or deleting. The `scripts/README_reddit_dataset.md` provides context but no timeline or status.

#### 12. .claude/settings.local.json Has Stale Paths
**Path:** `.claude/settings.local.json`
**Issue:** Several allow-rules reference paths or git show commands to deleted files:
- `Bash(rm -rf "/Users/matthewprowse/Documents/Development/Personal/Home Services/app/src/app/api/market-rates/" ...)`
- `Bash(git ... show HEAD:src/app/chat/components/unrelated-image-card.tsx)`
- `Bash(git ... show HEAD:src/app/chat/components/unserviced-category-card.tsx)`
- `Bash(git ... show HEAD:src/app/chat/components/providers-map.tsx)`
- `Bash(git ... show HEAD:src/app/chat/components/types.ts)`
**Details:** These references suggest the agent was configured to audit or restore deleted files. If these files no longer exist in HEAD, these permissions are stale.
**Severity:** LOW. Harmless but clutters the config. Recommend: Review and clean up stale references once you're confident those deletions were intentional.

#### 13. .claude/worktrees Has Orphaned Agent Worktrees (3)
**Path:** `.claude/worktrees/`
**Worktrees:**
- `agent-a5885d5072633b6f2/`
- `agent-aa386a7659b7882d0/`
- `agent-ad590185204142432/`
**Details:** These appear to be left-over worktrees from prior agent runs. If no longer active, they consume disk space and clutter the workspace.
**Severity:** LOW. Not harmful, but can be cleaned up with `git worktree remove <path>` if they're no longer in use.

#### 14. ~~Stale .env Content~~ — CORRECTED: `.env` is NOT committed
**Path:** `app/.env` (untracked — verified during report review)
**Details:** Original draft claimed `.env` was committed. This was a false positive. Verification: `git ls-files --error-unmatch .env` returns "did not match any file(s) known to git", and `.env` is properly listed in `app/.gitignore`. The file exists on disk only.
**Severity:** ~~LOW~~ → **N/A — not a finding.** No action required. The original concern about real secrets being exposed in the repo is unfounded.

#### 15. build.log and build-spec.txt (potentially stale)
**Path:** `app/build.log` (156 KB), `app/build-spec.txt` (14.9 KB)
**Details:** Build log is very large and contains build output. Build spec may be outdated documentation.
**Severity:** LOW. These can be safely deleted from the repo; they're not needed for the current build.

#### 16. tsconfig.json Excludes scripts/ but Includes **/*.mts
**Path:** `app/tsconfig.json`
**Config:**
```json
"exclude": ["node_modules", "scripts"],
"include": ["next-env.d.ts", "**/*.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts", "**/*.mts", ".next/dev/types/**/*.ts"]
```
**Details:** The `scripts/` directory is excluded, but `**/*.mts` is included (modern module syntax). This is inconsistent but not harmful — `.mts` files in app scripts won't be included anyway due to the exclude rule's priority.
**Severity:** LOW. No functional issue, but could be clearer.

#### 17. Unused npm Scripts (LOW severity, for reference)
**Scripts in package.json not directly called by CI or docs:**
- `dev:turbo` — duplicate of `dev`
- `dev:webpack` — variant for debugging
- `build` uses `--webpack` flag (possibly legacy or for debugging)
- `perf:match-flow` — performance testing script; likely intentional one-off
**Details:** These are not forbidden, just underutilized variants.
**Severity:** LOW. Acceptable for debugging and performance testing.

#### 18. Migration Naming Edge Case: 20260416_provider_application_pipeline.sql
**Path:** `app/supabase/migrations/20260416_provider_application_pipeline.sql`
**Issue:** Timestamp format is `YYYYMMDD_*` instead of the required `YYYYMMDDHHMMSS_*`.
**Details:** CLAUDE.md specifies `YYYYMMDDHHMMSS_description.sql`. This migration has only 8 digits for date, no time component.
**Severity:** MEDIUM → moved to LOW because it's a single instance and Supabase will apply it (lex order still works). Recommend: Rename to `20260416000000_provider_application_pipeline.sql` for consistency.

#### 19. vercel.json Cron Schedule Spacing Issue
**Path:** `app/vercel.json`
**Schedule:** `"0 */5 * * *"` for `process-provider-applications` (every 5 minutes)
**Details:** Not an error, but a very frequent cron (every 5 minutes) for a provider application processor. Confirm this is intentional; if not, consider reducing frequency.
**Severity:** LOW. Operational, not a code issue.

#### 20. CI Workflow (test.yml) Does Not Run All Checks
**Path:** `.github/workflows/test.yml`
**Missing from CI:**
- `pnpm check:diagnoses-table` (is defined in package.json, but CI doesn't run it)
**Details:** package.json defines a check for `conversations` table usage (which should use `diagnoses`). This check is not run in CI, so regressions could slip through.
**Severity:** LOW. Recommend: Add `- run: pnpm check:diagnoses-table` to CI before `lint`.

---

## Summary

**Findings by Severity:**
- **HIGH:** 3 (corrupt git refs, lockfile conflict, planning docs public)
- **MEDIUM:** 4 (migration duplicates, unreferenced email, missing scripts, env var mismatches)
- **LOW:** 12 (empty readme, font spaces, dead scripts, stale permissions, orphaned worktrees, build artifacts, naming inconsistencies, CI gaps, etc.)

**Total:** 19 distinct findings (some grouped by severity).

**Corrupt Git Ref Path:** `/Users/matthewprowse/Documents/Development/Personal/Home Services/app/.git/refs/heads/main 2` (and `main 3`, `main 4`)

**Lockfile Status:** Both `pnpm-lock.yaml` and `package-lock.json` exist; `package-lock.json` should be deleted.

**Public Planning Docs:** 3 files in `app/public/docs/planning/`; recommend moving to `app/docs/` if sensitive.

**Biggest Surprise:** `.env.example` references deprecated APIs (SendGrid) alongside current ones (Resend); environment variable documentation in CLAUDE.md is incomplete and out of sync with `.env.example`. (Original draft also flagged `app/.env` as committed — verified false: `.env` is in `.gitignore` and untracked. Finding #14 corrected accordingly.)

**Script/Email/Migration Surprises:**
- 4 TypeScript scripts in `app/scripts/` are completely unreferenced (dead code candidates).
- 1 email template (`monthly-digest-unregistered.tsx`) has no sender.
- 6 migration duplicates with OS-created " 2" suffix.

---

*Report generated by A6 audit phase.*
